# SENTINEL-FL — Python Dependencies
#
# Install options:
#
#   Minimal (Phase 0 demo only, ~10s):
#     pip install numpy scikit-learn scipy
#
#   Full production install (Phase 0 + backend API + dev tools):
#     pip install -e ".[dev]"
#
#   Phase 1 (PyTorch / Flower — heavy, GPU recommended):
#     pip install -e ".[phase1]"
#
# This file lists runtime dependencies for reference and CI reproducibility.
# Canonical dependency declarations live in pyproject.toml.

# ---------------------------------------------------------------------------
# Core scientific computing
# ---------------------------------------------------------------------------
numpy>=1.26
scikit-learn>=1.4
scipy>=1.12

# ---------------------------------------------------------------------------
# Data validation
# ---------------------------------------------------------------------------
pydantic>=2.6
pydantic-settings>=2.2

# ---------------------------------------------------------------------------
# Configuration
# ---------------------------------------------------------------------------
pyyaml>=6.0

# ---------------------------------------------------------------------------
# Backend API
# ---------------------------------------------------------------------------
fastapi>=0.110
uvicorn[standard]>=0.29

# ---------------------------------------------------------------------------
# HTTP client (used by FastAPI TestClient in tests)
# ---------------------------------------------------------------------------
httpx>=0.27

# ---------------------------------------------------------------------------
# Required visualisation (red-team evidence + explainability charts)
# ---------------------------------------------------------------------------
matplotlib>=3.8
pandas>=2.2

# ---------------------------------------------------------------------------
# Phase 1 — PyTorch + Flower (commented out; install via pip install -e "[phase1]")
# ---------------------------------------------------------------------------
# torch>=2.2
# torchvision>=0.17
# flwr>=1.8
