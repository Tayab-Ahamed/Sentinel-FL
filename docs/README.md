# docs/

**Purpose**: overflow for design notes that don't belong in the top-level docs
(`../README.md` indexes all top-level docs). Empty until something needs a home
here that isn't covered by an existing top-level document — deliberately not
pre-populated with placeholders.
