# datasets/

**Purpose**: dataset artifacts and partition manifests — **not raw data checked into
git** (see `../.gitignore`). Partitioning/generation *code* lives in
`ai/training/poison.py`; this folder holds resulting manifests/caches only.

**Contents**: `.gitignore`d at runtime; empty in the repository itself by design.
