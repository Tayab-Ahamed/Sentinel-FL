# tests/

**Purpose**: test suite per `../TESTING.md`. Mirrors the `ai/`/`backend/` package
structure (`tests/ai/fl_core/test_fl_engine.py`, etc.) — not yet populated
(`../IMPLEMENTATION_PLAN.md` Week 1 onward, alongside each module landing).
