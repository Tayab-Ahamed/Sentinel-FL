# ai/

All machine-learning and federated-learning code. Subpackages:
`fl_core/` (engine), `training/` (data + attacks), `detection/` (L1–L4 defenses),
`remediation/` (mitigation, planned). See each subfolder's README and
`../ARCHITECTURE.md` §7 for full subsystem detail.
