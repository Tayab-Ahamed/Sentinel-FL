# scripts/

**Purpose**: entry-point scripts — not library code (that's `ai/`, `backend/`).

**Contents**: `run_demo.py` — the Phase 0 end-to-end proof-of-concept
(`../RESULTS.md`). Future: `run_benchmark.py` (Week 4, executes `../BENCHMARK.md`'s
full matrix).
