# configs/

**Purpose**: YAML configuration files matching `../SCHEMAS.md#Configuration`, plus
`registry.yaml` (module registration, see `../INTERFACES.md` 'Registration
Convention').

**Contents (planned, Week 1+)**: `phase0_synthetic.yaml`, `phase1_official.yaml`,
`registry.yaml`.
