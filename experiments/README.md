# experiments/

**Purpose**: per-run artifacts — configs used, seeds, results JSON. See
`../SCHEMAS.md#Experiment` and `../BENCHMARK.md` for the reporting convention.

**Contents so far**: `demo_results.json` — output of `../scripts/run_demo.py`,
explained in `../RESULTS.md`.

**Convention**: one subfolder per `experiment_id` once the benchmark suite (Week 4)
is running; flat files until then.
